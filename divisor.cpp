#ifndef CPP_FOR_FINANCE_DIVISOR_CPP
#define CPP_FOR_FINANCE_DIVISOR_CPP

#include "divisor.h"
/*void type pour function when return nothing*/
double divisor(double num,int den)
{
    // input: double, int
    // output: return the division between the two inputs
    double div = num/den;
    return(div);
}
#endif